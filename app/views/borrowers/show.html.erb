<p> Name: <%= @user.first_name %> <%= @user.last_name %></p>
<p> Amount Needed: $<%= @user.money %></p>
<p> Amount Raised: $<%= @user.amount_raised %></p>

<div class="row">
	<h2> List of people who have lent you money </h2>
<table class="table table-striped">
	<thead>
		<tr>
			<td> Name </td>
			<td> Email </td>
			<td> Amount Lent </td>
		</tr>
	</thead>

	<tbody>
	<% @lenders.each do |lender | %>
		<tr>
			<td> <%= lender.user.first_name %> <%= lender.user.last_name %></td>
			<td> <%= lender.user.email %></td>
			<td> $<%= lender.amount_loaned %></td>
		</tr>
	<% end %>
	</tbody>
</table>
</div>