<header>
<nav class="navbar navbar-default">
	<div class="container">
		
	  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <li class="h2"> Lending </li>
      	<% if logged_in?  %>
        <li class="h3"><%= link_to ("Welcome " + current_user.first_name), current_user  %></li>

        </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.lender? %>
          <li><%= link_to "Dashboard", lender_path(current_user) %></li>
          <% else %>
          <li><%= link_to "Dashboard", borrower_path(current_user) %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Settings <span class="caret"></span></a>
          <ul class="dropdown-menu">

          <% if current_user.lender? %>
	        <li><%= link_to "Profile", lender_path(current_user) %></li>
          <% else %>
          <li><%= link_to "Profile", borrower_path(current_user) %></li>
          <% end %>
	        <li><%= link_to "Log out", logout_path, method: "delete" %></li>
          </ul>
        </li>
      
      <% else %>
        <li> <%= link_to "Login", login_path %> </li>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
    </div>
	</nav>

</header>